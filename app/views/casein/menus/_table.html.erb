<!-- Scaffolding generated by Casein v5.2.0.0 -->

<table class="table table-striped table-hover table-condensed">
  <thead>
    <tr>
      <th><%= casein_sort_link "Dish", :dish_id %></th>
      <th><%= casein_sort_link "Restaurant", :restaurant_id %></th>
      <th>&nbsp;</th>
    </tr>
  </thead>

  <% menus.each do |menu| %>
    <tr>
      <td><%= casein_table_cell_link menu.dish_id, casein_menu_path(menu) %></td>
      <td><%= casein_table_cell_link menu.restaurant_id, casein_menu_path(menu) %></td>
      <td class="delete">
        <%= link_to(casein_show_row_icon("trash"), casein_menu_path(menu), method: :delete, data: { confirm: "Are you sure you want to delete this menu?" }) %>
      </td>
    </tr>
  <% end %>
</table>
